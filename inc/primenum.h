/**
* @file primenum.h
*
*/
#ifndef __PRIMENUM_H__
#define __PRIMENUM_H__

/**
* Calculates the factorial of integer number
* @param[in] num to check whether a prime number or not
* @return true for prime and false for non prime
* @note Returns -1 for negative values
*/
bool primenum(int num);

#endif /* #ifndef __PRIMENUM_H__ */
